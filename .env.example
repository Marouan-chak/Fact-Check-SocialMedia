# Copy to .env and fill in your values. Do NOT commit .env.
OPENAI_API_KEY=sk-proj-REPLACE_ME

# Optional: Gemini API key (only required if TRANSCRIBE_MODEL is set to a Gemini model)
GEMINI_API_KEY=REPLACE_ME

# Optional: yt-dlp cookies file (needed for some private/age-gated IG content)
# YTDLP_COOKIES_FILE=/absolute/path/to/cookies.txt

# Optional: storage location for job artifacts
DATA_DIR=./data

# Models
# Examples: gpt-4o-transcribe, gemini-2.5-flash
TRANSCRIBE_MODEL=gpt-4o-transcribe
# If audio is longer than this, it will be split and transcribed in chunks, then concatenated.
TRANSCRIBE_CHUNK_SECONDS=900
# Parallel transcription workers for chunked audio (tune down if you hit rate limits).
TRANSCRIBE_MAX_WORKERS=3
FACTCHECK_MODEL=gpt-5.2-2025-12-11
# Examples (Gemini): gemini-2.5-flash, gemini-2.5-pro, gemini-3-pro-preview
